# AgentFlow Task — TASK-P01-001

```yaml
task_id: TASK-P01-001
title: Create HTML/CSS Scaffold for Salah Time App
type: feature
agent: codex
reviewer: claude
priority: high
cycle: 1

objective: >
  Create index.html and style.css for the Salah Time web app.
  This task covers only static layout — no JavaScript, no API calls.
  The result must be a valid HTML page that displays the prayer time UI
  structure correctly in a browser without any functionality.

acceptance_criteria:
  - index.html exists at repo root and opens in a browser without errors
  - style.css exists at repo root and is linked correctly in index.html
  - Page displays an app title (e.g. "Salah Time" or "เวลาละหมาด")
  - Page displays today's date placeholder (static text is acceptable)
  - Page displays a city/location name placeholder
  - Page displays exactly 5 prayer time rows with labels and time placeholders:
      Fajr (ซุบฮ์), Dhuhr (ซุฮ์ร), Asr (อัศร์), Maghrib (มัฆริบ), Isha (อิชาอ์)
  - One prayer row has a visual "active" or "next" highlight state (CSS class only)
  - Page is responsive: layout is usable on 375px (mobile) and 1024px (desktop)
  - No JavaScript is present in this task (no <script> tags, no .js files)
  - HTML validates without errors (W3C-level structure: doctype, lang, meta charset, viewport)

constraints:
  - No JavaScript in this task
  - No external CSS frameworks (no Bootstrap, no Tailwind)
  - No images or icon files required
  - CSS must be in style.css only — no inline styles
  - File names must be exactly: index.html and style.css at repo root
  - Do not create app.js or any other files in this task
  - Target browsers: modern Chrome/Firefox/Safari (no IE support needed)

test_requirements:
  - Open index.html directly in Chrome — page must render without console errors
  - Resize browser window to 375px width — all 5 prayer rows must remain readable
  - Resize browser window to 1024px width — layout must be centered and readable
  - Visually confirm: exactly 5 prayer rows are present with correct Thai/English labels
  - Visually confirm: one row has a distinct visual highlight (different background or border)
  - Run HTML through https://validator.w3.org/#validate_by_input — zero errors required

relevant_files:
  - index.html   ← CREATE at repo root
  - style.css    ← CREATE at repo root

architecture_notes:
  - Repo for this project: create new repo named "salah-time" (separate from agentflow repo)
  - No build tools — plain HTML/CSS only
  - Prayer time data will be injected by JavaScript in TASK-P01-002
    Use placeholder text like "--:--" for times and "Loading..." for city
  - CSS class naming convention: BEM or simple flat classes (e.g. .prayer-row, .prayer-row--active)
  - Color scheme: neutral/dark background preferred (night-friendly for prayer times)
  - Font: system font stack is acceptable, no Google Fonts required

git_instructions:
  - Create new repo: salah-time
  - Initial branch: main
  - Branch for this task: feature/TASK-P01-001-html-scaffold
  - Commit message: "TASK-P01-001: Add HTML/CSS scaffold for prayer time display"
  - Do not push or merge — report branch name and commit hash only
```

---

## Codex Readiness Check

- [x] Task has a single focused objective (layout only, no JS)
- [x] Acceptance criteria are visually verifiable and measurable
- [x] Constraints are explicit (no JS, no frameworks, no inline styles)
- [x] Test requirements are executable without test runner (manual browser check)
- [x] No missing requirements — all fields defined
- [x] No external service dependencies
- [x] Independently completable and reviewable
